# ไฟล์ระบุแพ็กเกจ Python ที่จำเป็นสำหรับโปรเจค Weather Regression
# ใช้สำหรับติดตั้ง dependencies ด้วยคำสั่ง: pip install -r requirements.txt

numpy>=1.21.0  # ไลบรารีสำหรับการคำนวณทางคณิตศาสตร์และจัดการอาร์เรย์
matplotlib>=3.5.0  # ไลบรารีสำหรับสร้างกราฟและการแสดงผลข้อมูลภาพ
scipy>=1.7.0  # ไลบรารีสำหรับการคำนวณทางวิทยาศาสตร์ รวมถึงการอ่านไฟล์ ARFF
flask>=3.1.0  # ไลบรารีสำหรับสร้างเว็บแอปพลิเคชัน
